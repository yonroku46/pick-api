server:
  address: localhost
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      force: true

spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: 'teambepo@gmail.com'
    password: 'omcxqpkflylisorz'
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            protocols: TLSv1.2
          starttls:
            enable: true
            required: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/public?allowPublicKeyRetrieval=true&useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: admin
    password: teamBEP0
  jpa:
    properties:
      hibernate:
        format_sql: true
        cache:
          use_second_level_cache: false
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    # 배포시 validate
    hibernate.ddl-auto: update

logging.level:
  org.hibernate:
    SQL: debug

#custom:
#  path:
#    img_upload_path: '/Desktop/img-resources/'